query GetProductBySlug($slug: String!) {
  product(where: { slug: $slug }) {
    description
    id
    name
    price
    categories {
      name
    }
    reviews {
      content
      name
    }
    quantity
    images {
      fileName
      url
    }
    slug
  }
}
