query GetProductBySlug($slug: String!) {
  product(where: { slug: $slug }) {
    description
    id
    name
    price
    reviews {
      content
      name
    }
    quantity
    images {
      fileName
      url
    }
    slug
  }
}
