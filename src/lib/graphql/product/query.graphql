query GetProductBySlug($slug: String!) {
  product(where: { slug: $slug }) {
    description
    id
    name
    price
    reviews {
      content
      name
    }
    images {
      fileName
      url
    }
    slug
  }
}
