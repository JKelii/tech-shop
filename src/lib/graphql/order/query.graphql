query GetOrders($email: String!) {
  orders(where: { account: { email: $email } }, stage: DRAFT) {
    total
    stripeCheckoutId
    createdAt
    orderStatus
    orderItems {
      product {
        slug
        name
        images {
          url
          fileName
        }
        price
      }
    }
  }
}
