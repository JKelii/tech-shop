query GetCartById($id: ID!) {
  cart(where: { id: $id }, stage: DRAFT) {
    cartProducts {
      id
      quantity
      product {
        slug
        name
        price
        images(first: 1) {
          url
        }
      }
    }
  }
}
