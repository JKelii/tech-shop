mutation CreateCart($quantity: Int!, $slug: String!, $email: String) {
  createCart(
    data: {
      cartProducts: {
        create: {
          quantity: $quantity
          product: { connect: { slug: $slug } }
          accounts: { connect: { email: $email } }
        }
      }
    }
  ) {
    id
  }
}
